# 2026.241.1545 — feat: declare Hyprland as a service and gate it through the wl verb

## Summary

Declares the nested compositor as a charly **`service:`** (plan §1.2 — one entry, rendered as a supervisord `[program:]` in a pod and a systemd unit on a VM), ordered by **B2's `wait_for:`** on the parent socket rather than a sleep, and pins `plugin-wl` via `require:` so `wl:` checks resolve.

**This diff is exactly one file and purely additive** — `charly.yml`, 33 additions, 0 deletions:

```
require:            @github.com/opencharly/plugin-wl/candy/plugin-wl:v2026.240.1326
service:            cstream-hyprland  (exec, env, restart, priority, scope, enable)
service.wait_for:   paths: ${XDG_RUNTIME_DIR}/wayland-1, timeout 30s
```

## Correcting my previous body

An earlier revision of this body claimed things this diff does not do. Withdrawing them:

- **"the shell probe is deleted"** — false. The probe was **bed-local** (a fixture in my scratch bed), never in this repo, so there is nothing here to delete. No R5 sweep is owed.
- **"all four fixes land here"** — false, and this is where each actually lives:

| fix | where it landed |
|---|---|
| `chmod 700` EPERM under `set -e` | the bed's own parent launcher — fixture, not shipped |
| `/run/cstream` not creatable by a non-root service | the bed's fixture, plus **this diff's** `XDG_RUNTIME_DIR: /tmp/cstream-rt` service env |
| missing `gst-plugin-va` → no `vapostproc` | **opencharly/layer-gst-wayland-display#2**, already merged |
| `WAYLAND_DISPLAY` never set for the child | **this diff**, in the service `env:` |

So two of the four are in this diff (both as service `env:` entries), one is in another repo and already merged, and one is fixture-only. The four are what the bed *uncovered*; they are not all what this PR *changes*. The tier below is set against what this diff actually contains.

## How tested

Disposable bed composing `layer-gst-wayland-display` (published tag `v2026.241.1323`) as the Wayland parent, with this candy gated **exactly as this repo ships it** — the bed's copy is generated from this `charly.yml` with only the project version stamp stripped:

```
charly check run check-nest: PASS (steps=13)
27 checks passed, 0 failed

wl: hypr-monitors    -> [{ ... "name": "WAYLAND-1", "width": 1920, "height": 1080 ... }]
wl: hypr-systeminfo  -> Hyprland 0.56.2 built from branch v0.56.2 at commit efb5099 clean
wl: hypr-workspaces  -> [{ ... }]
```

The `wl:` steps genuinely execute rather than skipping — the `require:` pin is what makes that true, and a skip would read as not-failed.

Also confirmed live in the same run, incidental to this PR: charly's **B9 warning** fired on a missing `supervisord` candy before any build, and **B2's `wait_for:`** rendered into a real until-loop with an honest timeout.

## The `service:` check defect — routed, with a name

charly's `service:` check verb returns **PASS for a supervisord program in `FATAL` state**. Both service checks passed while `supervisorctl status` showed `gwd-parent FATAL — Exited too quickly`.

Filed as **opencharly/charly#456**, with reproduction and expected behaviour. That issue is the named task; it is not folded into this PR because the fix belongs in charly's check-verb implementation, not in a candy manifest, and this PR's `wl:` checks fail correctly when the compositor is down (they did, four times, during development).

## Rulebook compliance

- **R0** — git-workflow and root-cause-analyzer loaded before the first tool call.
- **R1** — each bug the bed exposed was root-caused from its own measured symptom in the service log.
- **R2** — no parking: everything this diff is responsible for is in it, and the one surfaced defect outside its scope is routed to the named issue charly#456.
- **R3** — no duplication introduced; the `service:` entry is the single ordering mechanism, replacing nothing in this repo.
- **R4** — no workaround: `wait_for:` is the supervised ordering primitive rather than a sleep.
- **R5** — N/A for a deletion sweep: this diff is additive and there is no legacy path in this repo to remove. (My earlier claim otherwise is withdrawn above.)
- **R6** — no force-push, no direct push to `main` (now enforced by ruleset `21803847`, probe-verified), no hook bypass.
- **R9** — the charly binary was rebuilt from source and used for every run quoted.
- **R10** — disposable bed, fresh rebuild, `PASS (steps=13)`, 27/27 checks, `wl:` steps confirmed executing.
- **ADE** — non-empty `description:` and eight deterministic `check:` steps.
- **B16 placement** — CORRECT: a compositor's service definition belongs in the compositor's candy.

## Change classification

- **Change class:** candy config (one file, additive: `require:` + `service:` + `wait_for:`)
- **Verification gate:** `charly check run check-nest`, `disposable: true`, fresh rebuild, 27/27
- **Attribution tier:** `fully tested and validated` — the added service declaration and pin executed live on a disposable target against a fresh rebuild, and the `wl:` routing they enable is exercised end-to-end. Scoped to what this diff contains, per the accounting above.

*Assisted-by: Claude Code Anthropic Opus 5 (fully tested and validated)*